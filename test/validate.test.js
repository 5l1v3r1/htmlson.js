import { expect } from 'chai'
import transform from '../src/main'

describe('Validate', function() {
  assert('test for wrong data format', () => {
    const config = {
      data: {
      },
      headers: {
        1: 'my Header',
      },
    }
  })
})
